version: '3.9'

services:

  mysql:
    image: mysql:8.0
    container_name: historical_memory_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: historical_memory_platform
    ports:
      - "3306:3306"

  redis:
    image: redis:7
    container_name: historical_memory_redis
    restart: always
    ports:
      - "6379:6379"
